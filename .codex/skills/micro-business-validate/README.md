# Micro-Business Validate Skill

This skill runs the local micro-business validation pipeline and summarizes the current verdict from stored evidence.

Keep the skill aligned with `npm run validate` and the validation pipeline.
