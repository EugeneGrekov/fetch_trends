# Autocomplete Default Output

This directory tracks the phase that makes autocomplete output path generation automatic when `--out` is omitted.

Scope:
- CLI output path defaults
- filename formatting for autocomplete runs

Ownership:
- autocomplete CLI and its immediate file-output contract
