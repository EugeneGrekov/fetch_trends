# Diagnostics

This directory contains local health checks for operators.

Diagnostics cover:

- configuration
- SQLite health
- recent jobs
- collector availability
- command documentation
- report and artifact integrity

Diagnostic results should classify issues as `pass`, `warn`, `fail`, or `skip` with actionable messages.
