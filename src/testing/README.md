# Testing

This directory contains shared fakes, fixtures, and temporary database helpers for tests.

Use these helpers to keep tests deterministic and independent of live external services.

Do not store generated local databases or real API responses here unless they are deliberately sanitized fixtures.
