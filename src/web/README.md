# Web

This directory contains the local web interface.

Responsibilities:

- serve the local HTTP app
- route idea, job, evidence, report, and settings views
- expose local validation behavior without requiring hosted infrastructure

Keep web behavior aligned with CLI capabilities and local SQLite storage.
