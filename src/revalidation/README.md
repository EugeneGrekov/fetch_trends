# Revalidation

This directory detects stale evidence and queues or runs follow-up validation work.

Responsibilities:

- identify stale evidence
- schedule revalidation tasks
- run queued revalidation jobs
- summarize revalidation outcomes

Keep revalidation resumable and explicit about skipped work.
