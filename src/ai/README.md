# AI

This directory contains the bounded AI runner.

Responsibilities:

- load prompt templates from `prompts/`
- prepare local evidence payloads
- run Codex or fake AI providers
- parse and validate structured JSON output
- write AI run artifacts when enabled

AI outputs should summarize stored evidence, not invent unsupported market proof.
