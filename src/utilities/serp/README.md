# SERP Collector

This directory contains search results evidence collection types and adapters.

SERP evidence should capture pages, titles, snippets, and source metadata. Missing provider keys should produce explicit warnings rather than failing the whole validation job.
