# Utilities

This directory contains reusable evidence collectors and support utilities.

Current utility areas:

- `autocomplete/`
- `serp/`
- `reddit/`
- `youtube/`
- `reviews/`
- `competitors/`
- `external/`

Collectors should return source-attributed evidence and degrade safely when live providers are unavailable.
