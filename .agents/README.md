# Local Agent Assets

This directory contains local agent skills checked into this workspace.

These skills are not part of the validator runtime unless a task explicitly invokes them.

Avoid changing skill instructions unless the work is specifically about agent skills.
