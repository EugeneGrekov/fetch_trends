# Local Agent Skills

This directory contains local skills used by agents in this workspace.

Most skills here are general-purpose helpers, not fetch_trends product code.

Project-specific runtime code lives in `src/`.
